class a
{
private:
	int d;
	float b;
	const int k;
public:
	a(int x, float y, int c) :d(x), b(y), k(c);

};



float test(int a, int b)
{

}

void* ptr = &test(4, 5);


vector a;

a.push

10 sensors

class a
{
	value
		name;
};

class node
{
	struct MyStruct
	{
		char pn;
		int value_p;
		MyStruct* next;
	};

	void insert(class node* pointer, int value, char pn1)
	{
		MyStruct* temp;
		class node* minihead = pointer;
		*temp = (MyStruct*)malloc(sizeof(MyStruct));

		temp->pn = pn1;
		temp->value_p = value;
		temp->next = nullptr;

		if (minihead == nullptr)
		{
			pointer = temp;
		}


		while (temp)

	}
};

class node a;

MyStruct* head;

class base
{
public:
	draw(b, b);
	draw(r);

};

class rec :: class base;
{

}

obj.draw(b, h);










